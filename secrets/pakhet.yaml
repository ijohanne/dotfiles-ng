# Secrets for pakhet host
# TODO: Replace placeholder values with actual secrets and encrypt with sops

# Cloudflare API credentials for ACME DNS challenges
# Format: CLOUDFLARE_DNS_API_TOKEN=your_token_here
cloudflare_api_key: PLACEHOLDER_CLOUDFLARE_API_KEY

# MaxMind license key for GeoIP database downloads
maxmind_api_key: PLACEHOLDER_MAXMIND_API_KEY

# SSH private key for borg backup to rsync.net
backup_ssh_key: |
  PLACEHOLDER_SSH_PRIVATE_KEY

# Screeny JWT secret for token signing (generate with: openssl rand -hex 32)
screeny_jwt_secret: PLACEHOLDER_SCREENY_JWT_SECRET

# Screeny admin password
screeny_admin_password: PLACEHOLDER_SCREENY_ADMIN_PASSWORD
